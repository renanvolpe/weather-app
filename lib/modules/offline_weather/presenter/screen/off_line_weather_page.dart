import 'package:flutter/material.dart';

class OffLineWeatherPage extends StatelessWidget {
  const OffLineWeatherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("offLine part"),
    );
  }
}
